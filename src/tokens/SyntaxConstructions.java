package src.tokens;

/***
 * Список токенов для определения синтаксических конструкций, пока только EOL, в будущем возможно расширим.
 * */
public enum SyntaxConstructions implements Tokens {
    EOL // End Of Line - символизирует
}
